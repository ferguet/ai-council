fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
asyncpg==0.29.0
pytest==8.3.3
pytest-asyncio==0.24.0
python-multipart==0.0.12
pypdf==5.0.1
python-docx==1.1.2
openpyxl==3.1.5
# Trae un ffmpeg propio ya compilado, sin depender de que el sistema lo
# tenga instalado (en Render no lo tiene). Se usa para partir las clases
# largas en trozos antes de transcribirlas: la API de Whisper no acepta
# ficheros de mas de 25 MB, y una clase de dos horas los pasa de largo.
imageio-ffmpeg==0.5.1
# Lee un PDF con el FORMATO de cada palabra (negrita, tamaño, color), no
# solo el texto plano como pypdf. Hace falta para saber que ha resaltado
# el profesor en sus diapositivas.
pdfplumber==0.11.4
